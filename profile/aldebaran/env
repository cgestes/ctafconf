#!/bin/sh
##

# Always use ccache if it's installed
null_which ccache && env_set CC  "ccache gcc"
null_which ccache && env_set CXX "ccache g++"

alias_set na "naogentoo -c naogentoo-atom"
alias_set ng "naogentoo -c naogentoo-geode"
