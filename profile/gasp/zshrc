#!/bin/sh
#echo "aldebaran zshrc profile"TH=$PATH:/usr/lib/ccache/
PATH=$PATH:/Applications/Webots/webots.app/Contents/MacOS/
PATH=$PATH:/usr/local/Trolltech/Qt-4.8.2/bin/
# to put my python before mac's python
PATH=/usr/local/bin:$PATH
source /usr/local/bin/qicd.sh
# allow auto-completion from work dir
[ -d ~/work ] && cdpath+=~/work

