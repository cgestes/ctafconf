
user-profile.sh
===============

ctafconf_profiles:
  This variable aggregates different profiles.

ctafconf_prompt:
  This variable sets your shell configuration.
  You can try by doing:
    $prompt <configuration_name>
  You can find an example of prompt configuration here:
    etc/zsh/prompt_ctaf_setup

