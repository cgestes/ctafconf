#!/bin/sh
##CTAFCONF

#GNU ls is not the same as BSD ls... who is stupid?
alias_unset ll && alias_set ll 'ls -FlG'
alias_unset la && alias_set la 'ls -FlaG'
alias_unset l  && alias_set l  'ls -FaG'
alias_unset ls && alias_set ls 'ls -FaG'
alias_unset lg && alias_set lg 'ls -FG | grep'

