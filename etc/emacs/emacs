;;
;; .emacs for ctafconf in ~/.config/ctafconf/etc/emacs/emacs
;;
;; Made by Cedric GESTES
;; Login   <ctaf42@gmail.com>
;;
;; Started on  Sat Oct 16 12:48:04 1993 Cedric GESTES
;;
;;CTAFCONF

;;SOURCES:
;;http://wcube.epita.fr/~eole
;;http://www.happycoder.org
;;http://cedet.sf.net
;;http://ecb.sf.net
;;http://www.dotemacs.de
;;http://www.boblycat.org/~malc/.emacs
;;http://www.emacswiki.org

;;;PATH and .el file
(setq load-path (nconc '( "~/.config/ctafconf/etc/emacs/site-lisp/"
			  "~/.config/ctafconf/etc/emacs/site-lisp/ilisp/"
			  "~/.config/ctafconf/etc/emacs/site-lisp/ecb/"
			  "~/.config/ctafconf/etc/emacs/site-lisp/tuareg/"
			  "~/.config/ctafconf/etc/emacs/site-lisp/doxymacs/lisp"
			  "~/.config/ctafconf/etc/emacs/site-lisp/sieve/") load-path))


;;debug those file on error
(setq debug-on-error t)
;;;;This sets garbage collection to hundred times of the default.
;;;;Supposedly significantly speeds up startup time. (Seems to work
;;;;for me,  but my computer is pretty modern. Disable if you are on
;;;;anything less than 1 ghz).
(setq gc-cons-threshold 50000000)


(load-file "~/.config/ctafconf/etc/emacs/site-lisp/safe-load.el")

;;configuration file
(safe-load "~/.config/ctafconf/etc/emacs/emacs.config")

(safe-load "~/.config/ctafconf/perso/emacs.config")

(if (file-exists-p "~/.emacs.pre.el")
    (safe-load "~/.emacs.pre.el"))

(safe-load "~/.config/ctafconf/etc/emacs/color.emacs")

;;emacs settings
(safe-load "~/.config/ctafconf/etc/emacs/settings.emacs")

;;startup message
(safe-load "~/.config/ctafconf/etc/emacs/startup.emacs")

;;mode (ada,tuareg, ido, autotemplate,..)
(safe-load "~/.config/ctafconf/etc/emacs/mode.emacs")

;;usefull functions
(safe-load "~/.config/ctafconf/etc/emacs/functions.emacs")

;;screensaver,
;;(safe-load "~/.config/ctafconf/etc/emacs/misc.emacs")

;;cparse, cpcomment, cwarn, cedet, ecb, semantic
(safe-load "~/.config/ctafconf/etc/emacs/prog.emacs")

;;keys bindings
(safe-load "~/.config/ctafconf/etc/emacs/bindings.emacs")

;;test
;;(safe-load "~/.config/ctafconf/etc/emacs/test.emacs")


;;personal settings
(if (file-exists-p "~/.myemacs")
    (safe-load "~/.myemacs"))
(if (file-exists-p "~/.emacs.post.el")
    (safe-load "~/.emacs.post.el"))

;;print debug info
(safe-load-check)


;; (custom-set-variables
;;   ;; custom-set-variables was added by Custom -- don't edit or cut/paste it!
;;   ;; Your init file should contain only one such instance.
;;  '(ecb-options-version "2.32")
;;  '(speedbar-frame-parameters (quote ((minibuffer) (width . 20)
;;                                      (border-width . 0)
;;                                      (menu-bar-lines . 0)
;;                                      (tool-bar-lines . 0) (unsplittable . t) (set-background-color "black")))))

